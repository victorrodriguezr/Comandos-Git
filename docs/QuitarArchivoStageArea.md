# Quitar archivo de stage area

1. Para quitar un solo archivo de stage area, se debe hacer lo siguiente:

```bash
git rm HEAD -- PATH/TO/FILE.extension
```

2. Para quitar todos los cambios del stage area, se debe hacer lo siguiente:

```bash
git reset HEAD --
```